version: '3.8'

volumes:
  pg_data:
  static_volume:
  media_volume:

services:
  db:
    image: postgres:13.0-alpine
    container_name: foodgram_db
    volumes:
      - pg_data:/var/lib/postgresql/data/
    env_file:
      - ../.env  # Указываем путь к .env в корне проекта
    environment:
      POSTGRES_DB: foodgram_db
      POSTGRES_USER: foodgram_user
      POSTGRES_PASSWORD: foodgram_pass
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    image: swwwqqt/foodgram_backend:latest  # Замените на ваш образ
    container_name: foodgram_backend
    restart: always
    volumes:
      - static_volume:/app/static/
      - media_volume:/app/media/
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - ../.env
    environment:
      DB_HOST: db
      DB_PORT: 5432

  frontend:
    image: swwwqqt/foodgram_frontend:latest  # Замените на ваш образ
    container_name: foodgram_frontend
    volumes:
      - ../frontend:/app
    depends_on:
      - backend

  nginx:
    image: nginx:1.25.4-alpine
    container_name: foodgram_nginx
    ports:
      - "80:80"
      - "443:443"  # Для HTTPS при необходимости
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - static_volume:/var/html/static/
      - media_volume:/var/html/media/
      - ../frontend/build:/usr/share/nginx/html/
    depends_on:
      - backend
      - frontend
